<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Create a Planet</title>
</head>
<body>
  <%- include('../partials/_nav') %>
  <h1>Edit a New Planet!</h1>
  <form action="/planets/<%=planet._id%>?_method=PUT" method="POST">
    <label for="name">Name:</label>
    <input type="text" name="name" id="name" value="<%= planet.name %>" />

    <label for="hasMoon">Has Moons?</label>
    <input type="checkbox" name="hasMoon" id="hasMoon" <% if (planet.hasMoon) { %>checked<% } %> />

    <button type="submit">Edit Planet</button>
  </form>
  <a href="/planets/">Back to Planets</a>
</body>
</html>